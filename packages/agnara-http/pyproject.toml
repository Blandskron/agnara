[project]
name = "agnara-http"
version = "0.0.0"
description = "HTTP/ASGI exposure adapter for Agnara capabilities."
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["agnara"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/agnara_http"]
