# agnara-a2a

Agent-to-Agent exposure adapter. Owns Agent Card and skill projection, A2A tasks, streaming and version negotiation.

- Import package: `agnara_a2a`
- Depends on: `agnara-core`
- Must not import: sibling adapter packages

See `ARCHITECTURE.md` sections 3 and 4 for the package boundaries and the
allowed dependency graph.
